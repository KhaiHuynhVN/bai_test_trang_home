const showMenu = (state) => state.header.showMenu;
const showOverlay = (state) => state.header.showOverlay;
const isShowOpacity = (state) => state.header.isShowOpacity;
const isShowTranslate = (state) => state.header.isShowTranslate;
const isShowBackground = (state) => state.header.isShowBackground;

export { showMenu, showOverlay, isShowOpacity, isShowTranslate, isShowBackground };
