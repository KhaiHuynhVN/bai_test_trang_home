const history = (state) => state.navMain.history;
const readyNextAnimation = (state) => state.navMain.readyNextAnimation;
const nextAnimation = (state) => state.navMain.nextAnimation;
const readyPrevAnimation = (state) => state.navMain.readyPrevAnimation;
const prevAnimation = (state) => state.navMain.prevAnimation;

export { history, readyNextAnimation, nextAnimation, readyPrevAnimation, prevAnimation };
